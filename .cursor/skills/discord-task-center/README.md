# 已迁移

本 skill 已迁至仓库根目录 **`discord-task-center/`**，便于 ClawHub 发布。

- **ClawHub 发布**：发布根目录下的 `discord-task-center/`，见项目根 `PUBLISH.md`。
- **在 Cursor 中使用**：将根目录的 `discord-task-center` 复制到此处覆盖，或创建符号链接：
  ```bash
  cd /path/to/discord-task-skill/.cursor/skills
  rm -rf discord-task-center
  ln -s ../../discord-task-center discord-task-center
  ```
